# tuple()

# tuple cannot be mutable it means we can't change once it is created

numbers = (1,2,3,4,5,6,7,8)
print(type(numbers))


# count() - is used to count the particular item how many items available in the tuple
numbers = (1,2,3,3,3,3,34,5,6,7,8)
numbers.count(3)
print(numbers.count(3))

